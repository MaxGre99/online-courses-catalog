{
  "name": "createx-courses-catalog",
  "version": "1.0.0",
  "description": "Online courses catalog — Createx test task",
  "scripts": {
    "build": "sass scss/main.scss css/main.css --style=compressed --no-source-map",
    "watch": "sass scss/main.scss css/main.css --watch"
  },
  "devDependencies": {
    "sass": "^1.100.0"
  }
}
